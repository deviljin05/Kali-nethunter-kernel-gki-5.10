name: Build Custom GKI Kernel (Wild KSU + SUSFS + BBRv3 + NetHunter)
# ============================================================================
# Trimmed build: only the following features are included, per request.
# Everything else from the fuller revision (Zenith governor, SuiTune
# driver, ZRAM/LZ4HC, IO scheduler tweaks, memory/writeback tuning,
# F2FS/EXT4 tuning, HZ_300/SCHED_MC gaming configs, swappiness change)
# has been removed — not disabled, actually deleted from the workflow.
#
# Kept:
#  - SUSFS4KSU (file/mount/kstat hiding, uname/cmdline spoofing, etc.)
#  - KernelSU-Next (Wild KSU, dev-susfs branch)
#  - Baseband Guard (BBG) — plain upstream form, no recovery bypass,
#    enforces in every boot mode. Toggle via the enable_bbg input.
#  - BBRv3 TCP congestion control backport (falls back to mainline BBR
#    if the patch doesn't apply cleanly; default congestion-control
#    name is detected from source rather than assumed)
#  - Droidspaces container-runtime support (kABI-safe SYSVIPC +
#    POSIX_MQUEUE patches; the risky configs are only enabled if both
#    patches apply cleanly, to avoid a bootloop)
#  - NTSync
#  - Custom kernel branding (-BLAZE-KSUNv1-STABLE version suffix)
#  - Kali NetHunter support (NEW): just the set of Kconfig flags Kali's
#    own NetHunter kernel-builder guide lists as required/recommended
#    (HID + USB gadget/configfs for BadUSB & HID keyboard/mouse attacks,
#    USB mass storage gadget, cfg80211/mac80211 + wireless extensions,
#    netfilter/TPROXY for MITM tooling). Toggle via enable_nethunter.
#    No out-of-tree driver build, no chipset-specific adapter driver —
#    that part was dropped per request. These are plain Kconfig bool
#    symbols, so they don't touch struct layout and don't conflict with
#    the SUSFS/KSU patches or break the GKI KMI.
#
# NOTE ON CONFIG_NAMESPACES / CONFIG_NET_NS / CONFIG_CGROUPS:
# These are already default-y on virtually all GKI android12-5.10
# defconfigs, so explicitly forcing them below is a no-op in the normal
# case. They were added back in on explicit user request.
#
# NOTE ON CONFIG_USER_NS:
# Per user request, this build now DOES force CONFIG_USER_NS=y (set
# alongside the other namespace configs below, and included in the
# post-build verification step) so that Droidspaces' "User namespace"
# optional-feature check reports [✓] instead of [✗].
#
# NOTE ON SOURCE PINNING (os_patch_level):
# 'lts' resolves to the rolling common-android12-5.10-lts branch (always
# current, moves under you between runs).
#
# Anything else (e.g. android12-5.10.264_r00) is a GKI release TAG. That
# tag lives on the kernel/common repo itself, NOT on kernel/manifest — so
# it cannot be passed to `repo init -b`, which selects a branch/tag of the
# *manifest* repo (this is what caused "couldn't find remote ref
# refs/heads/android12-5.10.264_r00" — repo was looking for that ref on
# kernel/manifest, where it doesn't exist). Instead: repo init/sync always
# targets the normal common-android12-5.10[-lts] manifest branch (which
# gives us kernel/common shallow-cloned at HEAD), and THEN, if a specific
# tag was requested, we fetch and check out that exact tag inside
# kernel_workspace/common directly against the kernel/common remote. This
# pins the actual kernel source tree to that release regardless of what
# the manifest's HEAD currently points to. Default below is pinned to the
# android12-5.10.264 release build per explicit user request, so repeat
# runs are reproducible until you change the input.
#
# NOTE ON NetHunter:
# Only Kali's documented "recommended kernel configuration for NetHunter"
# Kconfig symbols are set below — nothing here is a source-level patch,
# so it can't conflict with the SUSFS patch, the KernelSU-Next setup, or
# the kABI-safe Droidspaces patches applied earlier. Chipset-specific
# external Wi-Fi adapter drivers (monitor mode / injection) are NOT
# included — those still need a matching out-of-tree module built
# separately for your specific adapter, which is deliberately left out
# of this workflow now.
# ============================================================================

on:
  workflow_dispatch:
    inputs:
      os_patch_level:
        description: "'lts' = rolling LTS branch. Or pin an exact GKI release tag, e.g. android12-5.10.264_r00"
        required: true
        default: "android12-5.10.264_r00"
      susfs_commit:
        description: "SUSFS commit hash (leave empty = latest on gki-android12-5.10 branch, i.e. latest SUSFS release)"
        required: false
        default: ""
      device_name:
        description: "Optional label for artifact naming"
        required: false
        default: "gki-custom"
      enable_bbg:
        description: "Enable Baseband Guard (BBG) — LSM that blocks unauthorized writes to modem/bootloader partitions, protects against brick from malicious root modules. Enforces in all boot modes, including recovery."
        required: true
        default: "true"
      enable_nethunter:
        description: "Enable Kali NetHunter's recommended kernel Kconfig (HID/USB-gadget/BadUSB/cfg80211/netfilter). No chipset-specific driver — external adapter monitor-mode drivers are not included."
        required: true
        default: "false"

env:
  VERSION: android12-5.10
  CCACHE_DIR: /home/runner/.ccache

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 150
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc || true
          sudo apt-get clean

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git curl bc bison flex libssl-dev \
            build-essential python3 python3-pip libncurses-dev \
            ccache zip unzip device-tree-compiler

      - name: Install repo tool
        run: |
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          echo "$HOME/bin" >> "$GITHUB_PATH"
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Set up ccache
        run: mkdir -p "${{ env.CCACHE_DIR }}"

      - name: Resolve manifest branch and optional pinned release tag
        run: |
          set -euo pipefail
          INPUT="${{ github.event.inputs.os_patch_level }}"
          if [ "${INPUT}" = "lts" ]; then
            MANIFEST_REF="common-${{ env.VERSION }}-lts"
            PIN_TAG=""
            echo "Tracking rolling LTS branch: ${MANIFEST_REF}"
          else
            # INPUT (e.g. android12-5.10.264_r00) is a release TAG that lives
            # on kernel/common, not a branch/tag of kernel/manifest. Sync the
            # manifest's normal dev branch so repo/build tooling is present,
            # then pin the common/ checkout to this exact tag afterward.
            MANIFEST_REF="common-${{ env.VERSION }}"
            PIN_TAG="${INPUT}"
            echo "Manifest branch for sync: ${MANIFEST_REF}"
            echo "Will pin kernel_workspace/common to release tag: ${PIN_TAG}"
          fi
          echo "MANIFEST_REF=${MANIFEST_REF}" >> "$GITHUB_ENV"
          echo "PIN_TAG=${PIN_TAG}" >> "$GITHUB_ENV"

      - name: Sync GKI kernel source
        run: |
          set -euo pipefail
          mkdir kernel_workspace && cd kernel_workspace
          repo init -u https://android.googlesource.com/kernel/manifest \
            -b "${MANIFEST_REF}" --depth=1
          repo sync -c --current-branch --no-clone-bundle --no-tags -j"$(nproc)"

      - name: Pin kernel/common to exact release tag (if requested)
        if: ${{ env.PIN_TAG != '' }}
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          # repo-managed checkouts don't always name the remote "origin" —
          # it's whatever the manifest's <remote> element defines (often
          # "aosp" for AOSP kernel manifests). Detect it instead of assuming.
          REMOTE_NAME="$(git remote | head -n1)"
          if [ -z "${REMOTE_NAME}" ]; then
            echo "::error::No git remote found in kernel_workspace/common — cannot fetch pinned tag"
            exit 1
          fi
          echo "Using remote '${REMOTE_NAME}' to fetch tag ${PIN_TAG}"
          git fetch --depth=1 "${REMOTE_NAME}" "refs/tags/${PIN_TAG}:refs/tags/${PIN_TAG}"
          git checkout "refs/tags/${PIN_TAG}"
          echo "Now on: $(git describe --tags --always) ($(git rev-parse HEAD))"

      - name: Detect kernel sublevel
        working-directory: kernel_workspace
        run: |
          SUBLEVEL="$(grep -m1 '^SUBLEVEL = ' common/Makefile | awk '{print $3}')"
          echo "Detected SUBLEVEL: $SUBLEVEL"
          echo "SUBLEVEL=${SUBLEVEL}" >> "$GITHUB_ENV"

      - name: Clone SUSFS4KSU (latest = current SUSFS release)
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android12-5.10 susfs4ksu
          cd susfs4ksu
          if [ -n "${{ github.event.inputs.susfs_commit }}" ]; then
            git checkout "${{ github.event.inputs.susfs_commit }}"
          fi
          echo "SUSFS_COMMIT=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
          echo "SUSFS commit in use: $(git rev-parse HEAD) — $(git log -1 --format=%s)"

      - name: Set SUSFS kernel configs
        run: |
          cd kernel_workspace/common/arch/arm64/configs
          {
            echo "CONFIG_KSU=y"
            echo "CONFIG_KSU_SUSFS=y"
            echo "CONFIG_KSU_SUSFS_SUS_PATH=y"
            echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y"
            echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y"
            echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y"
            echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y"
            echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y"
            echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y"
            echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y"
            echo "CONFIG_KSU_SUSFS_SUS_MAP=y"
          } >> gki_defconfig

      - name: Copy SUSFS kernel patches
        run: |
          cd kernel_workspace/common
          cp ../susfs4ksu/kernel_patches/fs/* ./fs/
          cp ../susfs4ksu/kernel_patches/include/linux/* ./include/linux/
          cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch ./

      - name: Apply android12-5.10 pre-patch compatibility fixes
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          if [ "${SUBLEVEL}" -le "43" ]; then
            perl -i -pe 's/(int|size_t)\s+this_len\s*=\s*min_t\s*\(\s*\1\s*,/size_t this_len = min_t(size_t,/' fs/proc/base.c
          fi
          if [ "${SUBLEVEL}" -le "117" ]; then
            sed -i '/^[[:space:]]*\/\*$/,/^[[:space:]]*u32 mask = mark->mask & IN_ALL_EVENTS;$/d' fs/notify/fdinfo.c
            perl -i -pe 's/\bmask,\s*mark->ignored_mask/inotify_mark_user_mask(mark)/g' fs/notify/fdinfo.c
            perl -i -pe 's/ignored_mask:%x/ignored_mask:0/g' fs/notify/fdinfo.c
          fi

      - name: Apply SUSFS kernel patch
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          if ! patch -p1 --fuzz=3 < 50_add_susfs_in_gki-android12-5.10.patch; then
            echo "::error::SUSFS patch failed to apply cleanly — .rej files below:"
            find . -name "*.rej"
            exit 1
          fi

      - name: Fix SUSFS set_nameidata argument mismatch
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          sed -i 's/set_nameidata(nd, old_dfd, fake_filename, NULL);/set_nameidata(nd, old_dfd, fake_filename);/g' fs/namei.c

      - name: Revert android12-5.10 pre-patch compatibility fixes
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          if [ "${SUBLEVEL}" -le "43" ]; then
            sed -i 's/^size_t this_len = min_t(size_t, count, PAGE_SIZE);$/int this_len = min_t(int, count, PAGE_SIZE);/' fs/proc/base.c
          fi
          if [ "${SUBLEVEL}" -le "117" ]; then
            perl -i -pe 's/^(\s+if \(inode\) \{)/$1\n\t\t\/\*\n\t\t * IN_ALL_EVENTS represents all of the mask bits\n\t\t * that we expose to userspace.  There is at\n\t\t * least one bit (FS_EVENT_ON_CHILD) which is\n\t\t * used only internally to the kernel.\n\t\t *\/\n\t\tu32 mask = mark->mask & IN_ALL_EVENTS;/m' fs/notify/fdinfo.c
            perl -i -pe 's/\binotify_mark_user_mask\(mark\)/mask, mark->ignored_mask/g' fs/notify/fdinfo.c
            perl -i -pe 's/ignored_mask:0/ignored_mask:%x/g' fs/notify/fdinfo.c
          fi

      - name: Apply SUSFS show_pad fix (older sublevels only)
        working-directory: kernel_workspace/common
        run: |
          if [ "${SUBLEVEL}" -le "209" ]; then
            sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c
          fi

      - name: Setup KernelSU-Next (Wild KSU, dev-susfs)
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          curl -LSs "https://raw.githubusercontent.com/pershoot/KernelSU-Next/dev-susfs/kernel/setup.sh" | bash -s dev-susfs

      - name: Fix KernelSU-Next (static.patch)
        working-directory: kernel_workspace/common/KernelSU-Next
        run: |
          set -euo pipefail
          curl -LSs -o static.patch \
            https://raw.githubusercontent.com/WildKernels/GKI_KernelSU_SUSFS/main/.github/actions/kernelsu/patches/static.patch
          patch -p1 --forward < static.patch || echo "::warning::static.patch did not apply — may already be fixed upstream, not fatal"

      - name: Install Baseband Guard (BBG)
        if: ${{ github.event.inputs.enable_bbg == 'true' }}
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash

      - name: Configure Baseband Guard LSM
        if: ${{ github.event.inputs.enable_bbg == 'true' }}
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' common/security/Kconfig

      - name: Verify BBG installation
        if: ${{ github.event.inputs.enable_bbg == 'true' }}
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          if grep -q "baseband_guard" common/security/Kconfig; then
            echo "✓ BBG registered in common/security/Kconfig"
            grep -n "baseband_guard" common/security/Kconfig || true
          else
            echo "::error::baseband_guard not found in common/security/Kconfig after setup — BBG install likely failed above"
            exit 1
          fi

      - name: Record BBG version
        if: ${{ github.event.inputs.enable_bbg == 'true' }}
        working-directory: kernel_workspace/Baseband-guard
        run: |
          BBG_COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")
          echo "BBG_COMMIT=${BBG_COMMIT}" >> "$GITHUB_ENV"
          echo "BBG commit: ${BBG_COMMIT}"

      - name: Enable BBG config
        if: ${{ github.event.inputs.enable_bbg == 'true' }}
        run: |
          cd kernel_workspace/common/arch/arm64/configs
          echo "CONFIG_BBG=y" >> gki_defconfig

      - name: Apply BBRv3 backport
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          git clone --depth=1 https://github.com/WildKernels/kernel_patches.git /tmp/wildpatches
          BBRV3_PATCH="/tmp/wildpatches/common/bbrv3/0001-net-tcp-backport-BBRv3-to-android12-5.10.patch"
          BBR_CONG_NAME="bbr"
          BBRV3_PATCH_APPLIED="false"
          if [ -f "$BBRV3_PATCH" ]; then
            if patch -p1 < "$BBRV3_PATCH"; then
              BBRV3_PATCH_APPLIED="true"
              # Detect the actual registered congestion-control algorithm
              # name from source instead of assuming "bbr3" — some backports
              # keep the struct named "bbr" and only bump an internal
              # version field, others register a distinct "bbr3" name.
              DETECTED="$(grep -rhoE '\.name\s*=\s*"[a-zA-Z0-9_]+"' net/ipv4/tcp_bbr*.c 2>/dev/null | grep -oE '"[a-zA-Z0-9_]+"' | tr -d '"' | head -n1 || true)"
              if [ -n "$DETECTED" ]; then
                BBR_CONG_NAME="$DETECTED"
              fi
            else
              echo "::warning::BBRv3 patch had rejects — check .rej files below (BBR v1 config below still gives working TCP congestion control even if this fails)"
              find . -name "*.rej"
            fi
          else
            echo "::warning::BBRv3 patch not found at expected path — repo structure may have changed. Skipping; BBR (v1) below still applies."
          fi
          echo "Detected default TCP congestion control name: ${BBR_CONG_NAME}"
          echo "BBRv3 patch applied: ${BBRV3_PATCH_APPLIED}"
          echo "BBR_CONG_NAME=${BBR_CONG_NAME}" >> "$GITHUB_ENV"
          # CONFIG_TCP_CONG_BBR3 is only a real Kconfig symbol once the
          # patch above has added it — setting it unconditionally would
          # leave a dangling/unknown config entry if the patch fails,
          # the same class of no-op bug as an unconditional feature flag.
          echo "BBRV3_PATCH_APPLIED=${BBRV3_PATCH_APPLIED}" >> "$GITHUB_ENV"

          if ! grep -qF 'int proc_dou8vec_minmax(' ./include/linux/sysctl.h; then
            for p in sysctl_add_proc_dou8vec_minmax.patch sysctl_fix_data-races_in_proc_dou8vec_minmax.patch; do
              PF="/tmp/wildpatches/common/bbrv3/$p"
              [ -f "$PF" ] && { patch -p1 < "$PF" || echo "::warning::$p did not apply cleanly"; }
            done
          fi

      - name: Set networking / TCP congestion configs
        run: |
          cd kernel_workspace/common/arch/arm64/configs
          {
            echo "CONFIG_TCP_CONG_ADVANCED=y"
            echo "CONFIG_TCP_CONG_BBR=y"
            echo "CONFIG_TCP_CONG_CUBIC=y"
            echo "CONFIG_TCP_CONG_BIC=y"
            echo "CONFIG_TCP_CONG_WESTWOOD=y"
            echo "CONFIG_TCP_CONG_HTCP=y"
            if [ "${BBRV3_PATCH_APPLIED}" = "true" ]; then
              echo "CONFIG_TCP_CONG_BBR3=y"
            fi
            echo "CONFIG_DEFAULT_TCP_CONG=\"${BBR_CONG_NAME}\""
            echo "CONFIG_NET_SCH_FQ=y"
            echo "CONFIG_NET_SCH_FQ_CODEL=y"
            echo "CONFIG_NET_SCH_CAKE=y"
          } >> gki_defconfig

      - name: Apply NTSync patches
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          if [ "${SUBLEVEL}" -le "208" ]; then
            curl -LSs -o /tmp/0003-lockdep-assert-none-held.patch \
              https://raw.githubusercontent.com/WildKernels/GKI_KernelSU_SUSFS/main/.github/actions/ntsync/patches/0003-lockdep-assert-none-held.patch
            patch -p1 -N < /tmp/0003-lockdep-assert-none-held.patch || echo "::warning::lockdep fix did not apply — may already be present"
          fi

          curl -LSs -o /tmp/ntsync_compat_android12-5.10.patch \
            https://raw.githubusercontent.com/WildKernels/kernel_patches/main/common/ntsync/ntsync_compat_android12-5.10.patch
          patch -p1 < /tmp/ntsync_compat_android12-5.10.patch || echo "::warning::ntsync compat patch had rejects — check .rej"

          curl -LSs -o /tmp/ntsync_base.patch \
            https://raw.githubusercontent.com/WildKernels/kernel_patches/main/common/ntsync/ntsync_base.patch
          patch -p1 < /tmp/ntsync_base.patch || echo "::warning::ntsync base patch had rejects — check .rej"

          echo "CONFIG_NTSYNC=y" >> arch/arm64/configs/gki_defconfig

      - name: Apply Droidspaces kABI-safe patches (SYSVIPC + POSIX_MQUEUE)
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          # Droidspaces (LXC-like container runtime) needs CONFIG_SYSVIPC,
          # CONFIG_IPC_NS and CONFIG_POSIX_MQUEUE. On a GKI kernel these
          # normally shift task_struct's memory layout and bootloop the
          # device against stock vendor modules — these two official
          # Droidspaces patches use the ANDROID_KABI_RESERVE padding slots
          # instead, so the struct layout stays ABI-stable. Both are
          # mandatory for kernel 5.10 (this build's VERSION); this is NOT
          # optional per upstream docs. If either fails to apply, we do
          # NOT enable the risky configs below — better to skip the
          # feature than ship a kernel that bootloops on real hardware.
          DROIDSPACES_KABI_OK="true"

          curl -LSs -o /tmp/droidspaces_sysvipc_kabi.patch \
            https://raw.githubusercontent.com/ravindu644/Droidspaces-OSS/main/Documentation/resources/kernel-patches/GKI/below-kernel-6.12/001.GKI-below-6.12-fix_sysvipc_kabi_6_7_8.patch
          if ! patch -p1 < /tmp/droidspaces_sysvipc_kabi.patch; then
            echo "::warning::Droidspaces SYSVIPC kABI patch had rejects — check .rej files below. Skipping CONFIG_SYSVIPC/CONFIG_IPC_NS/CONFIG_POSIX_MQUEUE to avoid a bootloop."
            find . -name "*.rej"
            DROIDSPACES_KABI_OK="false"
          fi

          curl -LSs -o /tmp/droidspaces_posix_mqueue_kabi.patch \
            https://raw.githubusercontent.com/ravindu644/Droidspaces-OSS/main/Documentation/resources/kernel-patches/GKI/below-kernel-6.12/002.5.10_or_lower_use_android_abi_padding_for_posix_mqueue.patch
          if ! patch -p1 < /tmp/droidspaces_posix_mqueue_kabi.patch; then
            echo "::warning::Droidspaces POSIX_MQUEUE kABI patch had rejects — check .rej files below. Skipping CONFIG_SYSVIPC/CONFIG_IPC_NS/CONFIG_POSIX_MQUEUE to avoid a bootloop."
            find . -name "*.rej"
            DROIDSPACES_KABI_OK="false"
          fi

          echo "Droidspaces kABI patches applied cleanly: ${DROIDSPACES_KABI_OK}"
          echo "DROIDSPACES_KABI_OK=${DROIDSPACES_KABI_OK}" >> "$GITHUB_ENV"

      - name: Set Droidspaces (container runtime) configs
        run: |
          cd kernel_workspace/common/arch/arm64/configs
          # This is the GKI-Exclusive list from Droidspaces' own docs, plus
          # CONFIG_NAMESPACES / CONFIG_NET_NS / CONFIG_CGROUPS / CONFIG_USER_NS
          # added back in on explicit user request. Upstream Droidspaces docs
          # note the first three are already default-on for virtually every
          # GKI defconfig, so forcing them here is normally a no-op — but
          # they're harmless to force explicitly (they don't touch struct
          # layout / kABI the way SYSVIPC/POSIX_MQUEUE do, so they don't
          # need the kABI-safe patches above). CONFIG_USER_NS is the same
          # story: it does not shift task_struct layout either, so forcing
          # it does not require the SYSVIPC/POSIX_MQUEUE kABI patches and is
          # NOT gated behind DROIDSPACES_KABI_OK. SYSVIPC/POSIX_MQUEUE/IPC_NS
          # remain gated on the kABI patches applying cleanly — that gating
          # is NOT removed, since forcing those two specifically without the
          # padding-slot patches is what actually causes a bootloop.
          if [ "${DROIDSPACES_KABI_OK}" = "true" ]; then
            {
              echo "CONFIG_SYSVIPC=y"
              echo "CONFIG_POSIX_MQUEUE=y"
              echo "CONFIG_IPC_NS=y"
            } >> gki_defconfig
          else
            echo "::warning::Skipping CONFIG_SYSVIPC/CONFIG_POSIX_MQUEUE/CONFIG_IPC_NS — Droidspaces kABI patches did not apply cleanly this run."
          fi
          {
            echo "CONFIG_NAMESPACES=y"
            echo "CONFIG_NET_NS=y"
            echo "CONFIG_CGROUPS=y"
            echo "CONFIG_PID_NS=y"
            echo "CONFIG_USER_NS=y"
            echo "CONFIG_DEVTMPFS=y"
            echo "CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y"
            echo "CONFIG_NETFILTER_XT_TARGET_REJECT=y"
            echo "CONFIG_NETFILTER_XT_TARGET_LOG=y"
            echo "CONFIG_NETFILTER_XT_MATCH_RECENT=y"
            echo "CONFIG_IP_SET=y"
            echo "CONFIG_IP_SET_HASH_IP=y"
            echo "CONFIG_IP_SET_HASH_NET=y"
            echo "CONFIG_NETFILTER_XT_SET=y"
            echo "CONFIG_TMPFS_POSIX_ACL=y"
            echo "CONFIG_TMPFS_XATTR=y"
          } >> gki_defconfig

      - name: Set NetHunter recommended kernel configs (safe merge, KMI-aware)
        if: ${{ github.event.inputs.enable_nethunter == 'true' }}
        working-directory: kernel_workspace/common/arch/arm64/configs
        run: |
          set -euo pipefail
          # ROOT CAUSE OF PREVIOUS BOOTLOOP: this step used to blindly
          # append every symbol with `>>`, which OVERRIDES whatever the
          # stock GKI defconfig already had — including flipping
          # CFG80211/MAC80211 from their stock "=m" (module) to "=y"
          # (built-in). Vendor's proprietary Wi-Fi driver ships as a
          # prebuilt vendor_dlkm .ko that depends on cfg80211/mac80211
          # being loadable modules with a matching exported-symbol ABI
          # (KMI). Forcing them built-in breaks that match, the vendor
          # Wi-Fi module fails to load, and on some devices that's fatal
          # early enough in boot to bootloop. This is a documented GKI
          # failure mode, not a random fluke.
          #
          # Fix: CFG80211/MAC80211/CFG80211_WEXT are no longer touched
          # at all — virtually every GKI defconfig already ships them
          # correctly for the device's own Wi-Fi to work, so there was
          # never a reason to set them ourselves.
          #
          # For everything else below: only APPEND a symbol if it is not
          # already present in gki_defconfig in any form (=y, =m, or
          # explicitly "is not set"). This never overrides a value the
          # stock defconfig intentionally set, so it can't reintroduce
          # the same class of bug for a symbol we don't know about yet.
          add_if_missing() {
            local sym="$1" val="$2"
            if grep -qE "^${sym}=|^# ${sym} is not set" gki_defconfig; then
              echo "skip ${sym} — already defined by stock gki_defconfig, not overriding"
            else
              echo "${sym}=${val}" >> gki_defconfig
              echo "add  ${sym}=${val}"
            fi
          }

          # --- HID: keyboard/mouse passthrough + HID-based attacks ---
          add_if_missing CONFIG_HID y
          add_if_missing CONFIG_USB_HID y
          add_if_missing CONFIG_HIDRAW y
          add_if_missing CONFIG_HID_LOGITECH y
          add_if_missing CONFIG_HID_LOGITECH_DJ y
          add_if_missing CONFIG_HID_MULTITOUCH y

          # --- USB gadget / ConfigFS: BadUSB (HID keystroke injection),
          #     mass storage gadget, MTP/PTP, RNDIS/NCM network gadget ---
          add_if_missing CONFIG_USB_CONFIGFS y
          add_if_missing CONFIG_USB_CONFIGFS_F_HID y
          add_if_missing CONFIG_USB_CONFIGFS_F_FS y
          add_if_missing CONFIG_USB_CONFIGFS_MASS_STORAGE y
          add_if_missing CONFIG_USB_CONFIGFS_F_MTP y
          add_if_missing CONFIG_USB_CONFIGFS_F_PTP y
          add_if_missing CONFIG_USB_CONFIGFS_F_ACC y
          add_if_missing CONFIG_USB_CONFIGFS_UEVENT y
          add_if_missing CONFIG_USB_CONFIGFS_NCM y
          add_if_missing CONFIG_USB_CONFIGFS_RNDIS y

          # --- Core networking for MITM / packet capture tooling ---
          add_if_missing CONFIG_INET y
          add_if_missing CONFIG_PACKET y
          add_if_missing CONFIG_PACKET_DIAG y
          add_if_missing CONFIG_TUN y
          add_if_missing CONFIG_IP_ADVANCED_ROUTER y
          add_if_missing CONFIG_IP_MULTIPLE_TABLES y
          add_if_missing CONFIG_IP_NF_MANGLE y
          add_if_missing CONFIG_IP_NF_TARGET_MASQUERADE y
          add_if_missing CONFIG_NETFILTER_XT_TARGET_TCPMSS y
          add_if_missing CONFIG_NETFILTER_XT_TARGET_TPROXY y
          add_if_missing CONFIG_NETFILTER_XT_MATCH_SOCKET y
          add_if_missing CONFIG_BRIDGE y
          add_if_missing CONFIG_BRIDGE_NETFILTER y

          # --- Core wireless framework. NOTE: CFG80211/MAC80211 are added
          #     as "=m" (module) here, matching stock GKI convention — NOT
          #     "=y" (built-in), which is what caused the earlier bootloop.
          #     add_if_missing still won't touch them if stock already
          #     defines them in any form, so this only fires on a defconfig
          #     that genuinely doesn't have them at all. ---
          add_if_missing CONFIG_WIRELESS y
          add_if_missing CONFIG_CFG80211 m
          add_if_missing CONFIG_MAC80211 m
          add_if_missing CONFIG_MAC80211_LEDS y

          # --- Bluetooth sniffing (external USB BT dongle) ---
          add_if_missing CONFIG_BT_HCIBTUSB m

          # --- External USB Wi-Fi adapter drivers, in-tree chipsets only
          #     (no out-of-tree build needed — these ship with mainline
          #     Linux, just need enabling). Only load when an adapter of
          #     that chipset is actually plugged into OTG. ---
          add_if_missing CONFIG_WLAN_VENDOR_ATH y
          add_if_missing CONFIG_ATH_COMMON m
          add_if_missing CONFIG_ATH9K_HTC m

          add_if_missing CONFIG_WLAN_VENDOR_REALTEK y
          add_if_missing CONFIG_RTL_CARDS m
          add_if_missing CONFIG_RTL8187 m
          add_if_missing CONFIG_RTL8192CU m

          add_if_missing CONFIG_WLAN_VENDOR_MEDIATEK y
          add_if_missing CONFIG_MT7601U m

      - name: Snapshot pre-build defconfig for post-build diff
        if: ${{ github.event.inputs.enable_nethunter == 'true' }}
        working-directory: kernel_workspace/common/arch/arm64/configs
        run: cp gki_defconfig /tmp/gki_defconfig.pre-build

      - name: Apply custom kernel branding
        working-directory: kernel_workspace/common
        run: |
          set -euo pipefail
          mv scripts/setlocalversion scripts/setlocalversion.orig
          chmod +x scripts/setlocalversion.orig
          cat > scripts/setlocalversion << 'SETLOCALVERSION_EOF'
          #!/bin/sh
          printf -- '-BLAZE-KSUNv1-STABLE\n'
          SETLOCALVERSION_EOF
          chmod +x scripts/setlocalversion

      - name: Disable strict defconfig verification
        working-directory: kernel_workspace
        run: sed -i 's/check_defconfig//' ./common/build.config.gki

      - name: Build kernel
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          export KBUILD_BUILD_USER="builder"
          export KBUILD_BUILD_HOST="ci"
          LTO=thin \
          BUILD_CONFIG=common/build.config.gki.aarch64 \
          USE_CCACHE=1 \
          CCACHE_DIR="${{ env.CCACHE_DIR }}" \
          build/build.sh -j"$(nproc)"

      - name: Verify namespace / cgroup configs made it into the final .config
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          # gki_defconfig only carries our *requested* setting; the value that
          # actually matters is whatever ended up in the .config the build
          # produced, since a vendor/board fragment merged in later by
          # build.sh can silently override it. Fail loudly here instead of
          # finding out from a "disabled" check on-device.
          # NOTE: CONFIG_USER_NS is now forced above, so it's included here
          # too — a vendor/board fragment silently disabling it should also
          # fail this check loudly rather than surface only as a Droidspaces
          # on-device [✗].
          CFG=$(find out -name ".config" | head -n1)
          if [ -z "$CFG" ]; then
            echo "::error::Could not find a compiled .config under kernel_workspace/out to verify"
            exit 1
          fi
          echo "Checking $CFG"
          FAILED="false"
          for SYM in CONFIG_NAMESPACES CONFIG_NET_NS CONFIG_PID_NS CONFIG_CGROUPS CONFIG_USER_NS; do
            if grep -q "^${SYM}=y$" "$CFG"; then
              echo "✓ ${SYM}=y confirmed in final .config"
            else
              echo "::error::${SYM} is NOT enabled in the final .config — something downstream (vendor/board fragment) is overriding gki_defconfig. Actual line:"
              grep -n "${SYM}" "$CFG" || echo "(no ${SYM} line found at all)"
              FAILED="true"
            fi
          done
          if [ "${FAILED}" = "true" ]; then
            exit 1
          fi

      - name: Verify NetHunter configs did not flip vendor-critical symbols
        if: ${{ github.event.inputs.enable_nethunter == 'true' }}
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          # CFG80211/MAC80211 are the two symbols that caused the earlier
          # bootloop (forced from stock "=m" to "=y"). We no longer touch
          # them, but this check confirms that for real, every run, so a
          # future change to this workflow can't silently reintroduce it.
          CFG=$(find out -name ".config" | head -n1)
          if [ -z "$CFG" ]; then
            echo "::error::Could not find compiled .config to verify"
            exit 1
          fi
          echo "Final module type for Wi-Fi-critical symbols (must stay '=m' or whatever stock already had — should be UNCHANGED from a build with enable_nethunter=false):"
          for SYM in CONFIG_CFG80211 CONFIG_MAC80211 CONFIG_WIRELESS CONFIG_ATH9K_HTC CONFIG_RTL8187 CONFIG_RTL8192CU CONFIG_MT7601U CONFIG_BT_HCIBTUSB; do
            grep -E "^${SYM}=|^# ${SYM} is not set" "$CFG" || echo "${SYM}: not set"
          done
          echo ""
          echo "CONFIG_CFG80211 / CONFIG_MAC80211 specifically: if either shows '=y' (built-in) instead of '=m' (module) or its previous stock value, STOP — do not flash. That mismatch is what breaks the vendor's prebuilt Wi-Fi vendor_dlkm module and causes a bootloop. The other symbols above (ATH9K_HTC, RTL8187, RTL8192CU, MT7601U, BT_HCIBTUSB) are new external-adapter drivers being added as modules — expected to show '=m', harmless either way since they're new capability, not an override of something the vendor's own Wi-Fi already used."

      - name: Locate built Image
        working-directory: kernel_workspace
        run: |
          set -euo pipefail
          IMG=$(find out -path "*dist/Image" | head -n1)
          if [ -z "$IMG" ]; then
            IMG=$(find out -name "Image" | head -n1)
          fi
          if [ -z "$IMG" ]; then
            echo "::error::No kernel Image found anywhere under kernel_workspace/out — build failed above"
            exit 1
          fi
          echo "Found Image at: $IMG"
          echo "IMG_PATH=kernel_workspace/${IMG}" >> "$GITHUB_ENV"

      - name: Package AnyKernel3
        run: |
          set -euo pipefail
          git clone --depth=1 https://github.com/WildKernels/AnyKernel3.git -b gki-2.0 ak3
          cp "${{ env.IMG_PATH }}" ak3/Image
          cd ak3
          zip -r9 -q "../${{ github.event.inputs.device_name }}-Blaze-android12-5.10-susfs${SUSFS_COMMIT:0:8}.zip" . -x ".git*"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.device_name }}-build
          path: "*.zip"
          retention-days: 14
